import { DecimalfixerPipe } from './decimalfixer.pipe';

describe('DecimalfixerPipe', () => {
  it('create an instance', () => {
    const pipe = new DecimalfixerPipe();
    expect(pipe).toBeTruthy();
  });
});
